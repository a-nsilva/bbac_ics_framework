# ============================================================================
# BBAC ICS Framework - Python Requirements
# Python 3.10 ONLY (ROS 2 Humble compatibility)
# ============================================================================

# ---------------------------------------------------------------------------
# Core numerical stack (ORDEM IMPORTA - instalar primeiro)
# ---------------------------------------------------------------------------
numpy>=1.23.0,<1.25.0    # Compatible: scipy sistema + sklearn + tensorflow
scipy>=1.10.0,<1.12.0    # Explicit version (evita conflito com apt)
pandas~=2.1              # Stable data manipulation

# ---------------------------------------------------------------------------
# Machine Learning
# ---------------------------------------------------------------------------
scikit-learn~=1.3.0      # Permite patches (1.3.1, 1.3.2) mas não 1.4.x

# ---------------------------------------------------------------------------
# Visualization
# ---------------------------------------------------------------------------
matplotlib~=3.8
seaborn~=0.13
plotly~=5.18

# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------
tqdm~=4.66
pyyaml~=6.0

# ---------------------------------------------------------------------------
# Development & Testing
# ---------------------------------------------------------------------------
pytest~=7.4

# ============================================================================
# 🚨 ROS2 NOTES (DO NOT IGNORE)
# ============================================================================
# - NEVER install rclpy via pip
# - rclpy MUST come from ROS2 system packages (apt)
# - Python 3.11+ is NOT compatible with ROS2 Humble
